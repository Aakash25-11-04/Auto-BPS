__pycache__/
*.pyc
.venv/
data/abps.db
data/abps.db-journal

# Real downloaded datasets (fetched via the curl commands in README.md §2) —
# large (schedules.json ~80MB) and reproducible, so they don't belong in git.
data/raw/*.json

# Trained model artifacts (Layer 3) — regenerated by POST /api/ml/train or
# scripts/train_models.py from the synthetic data generator; not source.
data/ml_models/

# Alembic occasionally leaves .pyc-adjacent cache dirs under migrations/
migrations/__pycache__/
